import flask
import pandas as pd

# You can use the following list of commands to create a virtual environment

# pip3 install virtualenv
# virtualenv myproject env

# After creating a virtual environmnet, depending upon the operating
# system, you can create a bash file and change the version of dependencies
# either upgrade or downgrade certain requirements.

# Moreover, you can see the version of the requirements by using
# "pip3 freeze" commands.

# Furthermore, you can list them down in a file using, the command:
# "pip3 freeze > requirements.txt"

# To further install all the requirements from a project
# pip3 install -r .\requirements.txt
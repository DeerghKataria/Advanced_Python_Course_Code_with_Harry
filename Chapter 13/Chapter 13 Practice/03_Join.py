specs = ["16GB Unififed RAM", "Pro XDR Display", "M2 Pro", "512GB SSD", "1080p Web Camera"]

sentence = " and ".join(specs)
print(sentence)

# The join keyword, uses the seprators in between and replaces it with the
# string that we mentioned like in this case it was " and ". You can 
# also use it cleverly and use it with "\n" as well.